# Copiez ce fichier en ".env" (jamais commité, voir .gitignore) et
# remplissez avec vos vraies valeurs.

PORT=4000

DB_HOST=localhost
DB_PORT=3306
DB_USER=carpoollite
DB_PASSWORD=changez-moi
DB_NAME=carpoollite

# Doit être identique à SYNC_API_KEY dans functions/.env
SYNC_API_KEY=changez-cette-cle-secrete
